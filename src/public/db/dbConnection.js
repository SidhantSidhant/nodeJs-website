const mongoose = require("mongoose");
const express = require("express");
const path = require("path");
const hbs = require("hbs");




module.exports = {mongoose : mongoose, express : express, path : path, hbs : hbs}

